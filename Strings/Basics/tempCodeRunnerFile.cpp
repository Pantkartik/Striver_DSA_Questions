#include<bits/stdc++.h>
using namespace std;

int main(){
    // to get the input string from user 
    char name[20];
  
    cin>>name;
    cout<<name;
    int len=0;
   
    //  to calculate the length of string 
   for(int i=0;name[i]!='\0',i++){
len++;
   } 

   cout<<len<<endl;
}