#include<bits/stdc++.h>
using namespace std;

int main(){
    // to get the input string from user 
    char name[20];
    cin>>name;
    cout<<name;


    //  to calculate the length of string 
    int len=name.size();
    cout<<len<<endl;
}